extends layout

block content
  h1= title

  div(class="table-responsive ")
    table(class="table table-striped table-hover")
      thead
        tr 
          th 名稱
          th 建立日期
          th 內容
          th
      tbody
        for article in blogs
          tr
            td=article.Username
            td=article.CreateDate
            td=article.Article
            td
              a(href="/users/message/" + article.id , class="btn btn-info") More


